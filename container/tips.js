const OVERFLOW = {
    requestCount: 'promise的方法使用次数过多，包括then、catch等等',
    resolveCount: `"pushResolve"绑定次数过多`
}

const OVERFLOW_EN = {
    requestCount: 'The method of promise is used too many times, including then, catch, etc.',
    resolveCount: 'Too many pushResolve bound'
}

export {
    OVERFLOW, OVERFLOW_EN
}